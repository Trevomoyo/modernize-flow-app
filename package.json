{
  "name": "modernizeflow",
  "version": "1.0.0",
  "description": "ModernizeFlow website with Netlify Functions",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site - no build needed'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "nodemailer": "^6.9.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}